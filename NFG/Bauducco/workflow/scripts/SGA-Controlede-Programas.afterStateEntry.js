function afterStateEntry(sequenceId){

	/*
	log.info("### ENTROU no afterStateEntry");

	var WKNumProces = getValue("WKNumProces");
	var WKNumState = getValue('WKNumState');

	log.info('*** afterStateEntry sequenceId:' + sequenceId);
	log.info('*** afterStateEntry WKNumState:' + WKNumState);

	if(sequenceId == '29'){

		log.info("### afterStateEntry tarefa Analisa TI");

		if(hAPI.getCardValue('recebeCod1') == 'ERRO'){

			log.info("### afterStateEntry entrou no segundo IF");
			hAPI.setTaskComments("admin", numProcesso,  0, "NÃO FOI POSSIVEL ENCONTRAR O GESTOR DO PROGRAMA");
		}
	}
	*/
}